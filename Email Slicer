email = input('Enter your email address: ').strip()
    
username = email[:email.index('@')] #list_name.index(element, start, end)
    
domain = email[email.index('@')+1:]
    
result =  "Your Username is {}\nYour domain is {}".format(username,domain)
print(result)
